<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    mc:Ignorable="d">

    <Style TargetType="{x:Type DetailsViewGameOverview}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DetailsViewGameOverview}">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto"
                                  Style="{DynamicResource DetailsScrollViewer}"
                                  d:DesignWidth="1280"
                                  d:DesignHeight="1280"
                                  d:DataContext="{x:Static DesignMainViewModel.DesignSelectedGameDetailsIntance}">
                        <Grid>
                            <ContentControl x:Name="DuplicateHider_ContentControl" Panel.ZIndex="1"
                                            VerticalAlignment="Top" 
                                            HorizontalAlignment="Center" 
                                            Margin="0,10,0,0" 
                                            Tag="{DynamicResource DuplicateHider_ContentControlHeader_Style}" />

                            <StackPanel Name="PART_ImageContener" Tag="{DynamicResource DE_LimitBackgroundHeight}">
                                <StackPanel.Style>
                                    <Style TargetType="{x:Type StackPanel}">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{PluginSettings Plugin=BackgroundChanger, Path=HasDataBackground, FallbackValue=False}" Value="True" />
                                                    <Condition Binding="{PluginSettings Plugin=BackgroundChanger, Path=EnableBackgroundImage, FallbackValue=False}" Value="True" />
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <FadeImage x:Name="PART_ImageBackground" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Top">
                                    <FadeImage.Style>
                                        <Style TargetType="{x:Type FadeImage}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=PART_ImageContener, Path=Tag}" Value="True">
                                                    <Setter Property="Height" Value="{DynamicResource DE_BackgroundHeight}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </FadeImage.Style>

                                    <FadeImage.OpacityMask>
                                        <LinearGradientBrush StartPoint="0.5, 0.0" EndPoint="0.5, 1.0">
                                            <GradientStop Color="Black" Offset="0.75"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </LinearGradientBrush>
                                    </FadeImage.OpacityMask>
                                </FadeImage>
                            </StackPanel>

                            <!-- BackgroundChanger plugin background -->
                            <ContentControl x:Name="BackgroundChanger_PluginBackgroundImage" HorizontalAlignment="Center" VerticalAlignment="Top">
                                <ContentControl.Style>
                                    <Style TargetType="{x:Type ContentControl}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=PART_ImageContener, Path=Tag}" Value="True">
                                                <Setter Property="Height" Value="{DynamicResource DE_BackgroundHeight}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto" />
                                        <RowDefinition Height="auto" />
                                    </Grid.RowDefinitions>

                                    <!-- CheckLocalizations flags -->
                                    <StackPanel HorizontalAlignment="Right" Margin="0,10,20,0" Grid.Row="0" 
                                                Tag="{PluginSettings Plugin=CheckLocalizations, Path=EnableIntegrationFlags}">
                                        <StackPanel.Style>
                                            <Style TargetType="{x:Type StackPanel}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>

                                        <Border Padding="5" VerticalAlignment="Top"
                                                BorderBrush="{DynamicResource NormalBorderBrush}"
                                                CornerRadius="{DynamicResource ControlCornerRadius}"
                                                BorderThickness="{DynamicResource ControlBorderThickness}" 
                                                Background="{DynamicResource ButtonBackgroundBrush}"  
                                                Tag="{PluginSettings Plugin=CheckLocalizations, Path=HasData}">
                                            <Border.Style>
                                                <Style TargetType="{x:Type Border}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="False">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource LOCCheckLocalizationsLocalizations}" FontSize="14"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           Margin="5,0,5,0" VerticalAlignment="Center"
                                                           Visibility="{DynamicResource CL_ShowLocalizationsLabel}" />
                                                <ContentControl x:Name="CheckLocalizations_PluginFlags" Height="26" />
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>

                                    <!-- LibraryManagement plugin features list -->
                                    <StackPanel Margin="0,10,20,0" HorizontalAlignment="Right" Grid.Row="1" Grid.Column="1"
                                                Tag="{PluginSettings Plugin=LibraryManagement, Path=EnableIntegrationFeatures}">
                                        <StackPanel.Style>
                                            <Style TargetType="{x:Type StackPanel}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>

                                        <Border Padding="5" VerticalAlignment="Top"
                                                BorderBrush="{DynamicResource NormalBorderBrush}"
                                                CornerRadius="{DynamicResource ControlCornerRadius}"
                                                BorderThickness="{DynamicResource ControlBorderThickness}" 
                                                Background="{DynamicResource ButtonBackgroundBrush}"  
                                                Tag="{PluginSettings Plugin=LibraryManagement, Path=HasData}">
                                            <Border.Style>
                                                <Style TargetType="{x:Type Border}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="False">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource LOCFeaturesLabel}" FontSize="14"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           Margin="5,0,5,0" VerticalAlignment="Center"
                                                           Visibility="{DynamicResource LM_ShowFeaturesLabel}" />
                                                <ContentControl x:Name="LibraryManagement_PluginFeaturesIconList" Height="26" />
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </Grid>

                                <!-- Age Ratings -->
                                <ItemsControl ItemsSource="{Binding Game.AgeRatings}" Margin="5,10,0,0"
                                              Grid.Column="0"
                                              Visibility="{DynamicResource DE_ShowAgeRating}"
                                              VerticalAlignment="Top" HorizontalAlignment="Left"
                                              IsTabStop="False" IsEnabled="False">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" Height="Auto"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="auto" />
                                                </Grid.ColumnDefinitions>
                                                
                                                <Border x:Name="RatingIcon" Background="White" CornerRadius="5"
                                                        HorizontalAlignment="Center" VerticalAlignment="Center"
                                                        Height="32" BorderThickness="2"
                                                        Width="Auto"
                                                        Margin="1" Visibility="Visible">
                                                    <Image Grid.Column="0" x:Name="RatingIconImage"
                                                           RenderOptions.BitmapScalingMode="Fant"
                                                           SnapsToDevicePixels="True"
                                                           Margin="3"
                                                           Source="{ThemeFileBinding Name, PathFormat='Images/AgeRatings/{0}.png'}" />
                                                </Border>
                                            </Grid>

                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=RatingIconImage, Path=Source}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- LibraryManagement plugin features list -->
                                <ContentControl x:Name="LibraryManagement_PluginAgeRating" Height="50"
                                                Margin="5,10,0,0" Grid.Column="0"
                                                VerticalAlignment="Top" HorizontalAlignment="Left" />
                            </Grid>

                            <Grid HorizontalAlignment="Stretch" Margin="20,0,20,0" MaxWidth="1280" >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="{Settings CalculatedGameDetailsIndentation}"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <DockPanel Margin="0,20,0,15" Grid.Row="1" Background="Transparent">
                                    <StackPanel DockPanel.Dock="Right" VerticalAlignment="Bottom">
                                        <StackPanel.Style>
                                            <Style TargetType="{x:Type StackPanel}">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{PluginSettings Plugin=BackgroundChanger, Path=HasDataCover, FallbackValue=False}" Value="True" />
                                                            <Condition Binding="{PluginSettings Plugin=BackgroundChanger, Path=EnableCoverImage, FallbackValue=False}" Value="True" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>

                                        <Image Name="PART_ImageCover" VerticalAlignment="Bottom"
                                               Height="{Settings GameDetailsCoverHeight}"
                                               StretchDirection="Both" Stretch="Uniform"
                                               RenderOptions.BitmapScalingMode="Fant" />
                                    </StackPanel>

                                    <!-- BackgroundChanger plugin cover -->
                                    <ContentControl x:Name="BackgroundChanger_PluginCoverImage"
                                                    DockPanel.Dock="Right" VerticalAlignment="Bottom"
                                                    Visibility="{PluginStatus Plugin=playnite-backgroundchanger-plugin, Status=Installed}"
                                                    Height="{Settings GameDetailsCoverHeight}" />

                                    <StackPanel VerticalAlignment="Bottom" DockPanel.Dock="Left">
                                        <DockPanel>
                                            <Grid Name="PART_GridImageContener"
                                                  MaxHeight="48" MaxWidth="48"
                                                  DockPanel.Dock="Left" Margin="0,0,10,0"
                                                  VerticalAlignment="Center"
                                                  Visibility="{Binding ElementName=PART_ImageIcon, Path=Visibility}"
                                                  Tag="{PluginSettings Plugin=ThemeModifier, Path=EnableIntegrationIcon}">
                                                <Grid>
                                                    <Grid.Style>
                                                        <Style TargetType="{x:Type Grid}">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ElementName=PART_GridImageContener, Path=Tag}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Grid.Style>

                                                    <Image Name="PART_ImageIcon" RenderOptions.BitmapScalingMode="Fant" />
                                                </Grid>

                                                <!-- ThemeModifier view item -->
                                                <ContentControl x:Name="ThemeModifier_PluginIcon">
                                                    <ContentControl.Style>
                                                        <Style TargetType="{x:Type ContentControl}">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ElementName=PART_GridImageContener, Path=Tag}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ContentControl.Style>
                                                </ContentControl>
                                            </Grid>

                                            <TextBlock FontFamily="{DynamicResource FontFamily}"
                                                       Text="{Binding Game.DisplayName}" DockPanel.Dock="Left"
                                                       FontSize="{DynamicResource FontSizeLargest}" Margin="0,0,10,0" TextWrapping="Wrap"
                                                       VerticalAlignment="Center"
                                                       Foreground="{DynamicResource TextBrush}">
                                                <TextBlock.Effect>
                                                    <DropShadowEffect ShadowDepth="3" RenderingBias="Quality"/>
                                                </TextBlock.Effect>
                                            </TextBlock>
                                        </DockPanel>

                                        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Margin="0,15,0,0">
                                            <Button Name="PART_ButtonPlayAction"
                                                    Width="150" Height="40" Style="{DynamicResource PlayButton}" />
                                            <Button Name="PART_ButtonContextAction" 
                                                    Width="150" Height="40" />
                                            <Button Name="PART_ButtonMoreActions"
                                                    Content="{DynamicResource LOCMoreAction}"
                                                    Width="150" Height="40" Margin="10,0,0,0" />
                                            <Button Name="PART_ButtonEditGame"
                                                    Margin="10,0,0,0" Height="40" Focusable="False"
                                                    Content="&#xeded;" FontFamily="{DynamicResource FontIcoFont}">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                        <Setter Property="Visibility" Value="Hidden" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=DockPanel}}"  Value="True">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>

                                            <!-- CheckLocalizations plugin button -->
                                            <ContentControl x:Name="CheckLocalizations_PluginButton" Height="40" Margin="10,0,0,0" FontSize="14"
                                                            MinWidth="{Binding ElementName=PART_ButtonEditGame, Path=ActualWidth}"
                                                            Visibility="{PluginSettings Plugin=CheckLocalizations, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <!-- CheckDlc plugin button -->
                                            <ContentControl x:Name="CheckDlc_PluginButton" Height="40" Margin="10,0,0,0" FontSize="14"
                                                            MinWidth="{Binding ElementName=PART_ButtonEditGame, Path=ActualWidth}"
                                                            Visibility="{PluginSettings Plugin=CheckDlc, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <!-- SuccessStory Button -->
                                            <ContentControl x:Name="SuccessStory_PluginButton" Height="40" Margin="10,0,0,0" FontSize="18" 
                                                            MinWidth="{Binding ElementName=PART_ButtonEditGame, Path=ActualWidth}"
                                                            MaxWidth="150"
                                                            Visibility="{PluginSettings Plugin=SuccessStory, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <!-- ScreenshotsVisualizer plugin button -->
                                            <ContentControl x:Name="ScreenshotsVisualizer_PluginButton" Height="40" Margin="10,0,0,0" FontSize="16"
                                                            MinWidth="{Binding ElementName=PART_ButtonEditGame, Path=ActualWidth}"
                                                            Visibility="{PluginSettings Plugin=ScreenshotsVisualizer, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <!-- HowLongToBeat plugin button -->
                                            <ContentControl x:Name="HowLongToBeat_PluginButton" Height="40" Margin="10,0,0,0" FontSize="14"
                                                            MinWidth="{Binding ElementName=PART_ButtonEditGame, Path=ActualWidth}"
                                                            Visibility="{PluginSettings Plugin=HowLongToBeat, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <!-- GameActivity Button -->
                                            <ContentControl x:Name="GameActivity_PluginButton" Height="40" Margin="10,0,0,0" FontSize="16" 
                                                            MinWidth="{Binding ElementName=PART_ButtonEditGame, Path=ActualWidth}"
                                                            MaxWidth="150"
                                                            Visibility="{PluginSettings Plugin=GameActivity, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <!-- SystemChecker plugin button -->
                                            <ContentControl x:Name="SystemChecker_PluginButton" Height="40" Margin="10,0,0,0" FontSize="16"
                                                            MinWidth="{Binding ElementName=PART_ButtonEditGame, Path=ActualWidth}"
                                                            Visibility="{PluginSettings Plugin=SystemChecker, Path=HasData, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <!-- GameActivity AvgFps -->
                                            <Grid Name="PART_GaFpsContener" Tag="{PluginSettings Plugin=GameActivity, Path=AvgFpsAllSession}" />
                                            <StackPanel Margin="10,0,0,0" Tag="{PluginStatus Plugin=playnite-gameactivity-plugin, Status=Installed}">
                                                <StackPanel.Style>
                                                    <Style TargetType="{x:Type StackPanel}">
                                                        <Setter Property="Visibility" Value="{DynamicResource GA_ShowAvgFps}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding ElementName=PART_GaFpsContener, Path=Tag}" Value="0">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </StackPanel.Style>

                                                <Border Padding="5" Height="40"
                                                        BorderBrush="{DynamicResource NormalBorderBrush}"
                                                        CornerRadius="{DynamicResource ControlCornerRadius}"
                                                        BorderThickness="{DynamicResource ControlBorderThickness}" 
                                                        Background="{DynamicResource ButtonBackgroundBrush}">
                                                    <Border.Style>
                                                        <Style TargetType="{x:Type Border}">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="False">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>

                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">
                                                        <Label Content="{DynamicResource LOCGameActivityAvgFps}" HorizontalAlignment="Center" />
                                                        <Label Margin="10,0,0,0" 
                                                               Content="{PluginSettings Plugin=GameActivity, Path=AvgFpsAllSession}"
                                                               HorizontalAlignment="Center" />
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>

                                <DockPanel Grid.Row="2">
                                    <StackPanel DockPanel.Dock="Left" Width="380" Margin="2,0,0,0">
                                        <StackPanel.Resources>
                                            <Style TargetType="ItemsControl">
                                                <Setter Property="VerticalAlignment" Value="Center" />
                                            </Style>
                                            <Style TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
                                                <Setter Property="Margin" Value="0,4,8,4" />
                                                <Setter Property="VerticalAlignment" Value="Top" />
                                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                                            </Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource PropertyItemButton}" />
                                        </StackPanel.Resources>

                                        <TextBlock Text="{DynamicResource LOCGameDetails}" Margin="0,15,0,5"  Style="{DynamicResource BaseTextBlockStyle}" />

                                        <Separator Background="{DynamicResource PanelSeparatorBrush}" />

                                        <GridEx Margin="0,5,0,10" ColumnCount="2" StarColumns="1" RowCount="25" AutoLayoutColumns="2" x:Name="PART_GridEx">
                                            <Grid.Resources>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}" />
                                                <Style x:Key="Icon" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
                                                    <Setter Property="Width" Value="24"/>
                                                    <Setter Property="FontWeight" Value="Normal"/>
                                                </Style>
                                            </Grid.Resources>

                                            <Label Name="PART_ElemAdded">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEC45;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCDateAddedLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <TextBlock Name="PART_TextAdded" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemPlayTime">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEEDC;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCTimePlayed}" />
                                                </StackPanel>
                                            </Label>
                                            <TextBlock Name="PART_TextPlayTime" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemLastPlayed">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEC45;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCLastPlayed}" />
                                                </StackPanel>
                                            </Label>
                                            <TextBlock Name="PART_TextLastActivity" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemCompletionStatus">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEED6;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCCompletionStatus}" />
                                                </StackPanel>
                                            </Label>
                                            <Button Name="PART_ButtonCompletionStatus" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemInstallDirectory">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEC5B;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCGameInstallDirTitle}" />
                                                </StackPanel>
                                            </Label>
                                            <Button Name="PART_ButtonInstallDirectory" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemInstallSize">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xef43;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCInstallSizeLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <TextBlock Name="PART_TextInstallSize" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemLibrary">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEF65;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCGameProviderTitle}" />
                                                </StackPanel>
                                            </Label>
                                            <Button Name="PART_ButtonLibrary" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemPlatform">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEEEE;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCGamePlatformTitle}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsPlatforms" />

                                            <Label Name="PART_ElemGenres">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEF74;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCGenreLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsGenres"  />

                                            <Label Name="PART_ElemDevelopers">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEEF3;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCDeveloperLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsDevelopers" />

                                            <Label Name="PART_ElemPublishers">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEEF3;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCPublisherLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsPublishers" />

                                            <Label Name="PART_ElemReleaseDate">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEC45;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCGameReleaseDateTitle}" />
                                                </StackPanel>
                                            </Label>
                                            <Button Name="PART_ButtonReleaseDate" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemCategories">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEF74;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCGameCategoriesTitle}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsCategories" />

                                            <Label Name="PART_ElemFeatures">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEF74;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCFeaturesLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsFeatures" Tag="{DynamicResource DE_LimitFeaturesHeight}"
                                                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                                                <ItemsControl.Style>
                                                    <Style TargetType="{x:Type ItemsControl}" BasedOn="{StaticResource {x:Type ItemsControl}}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ElementName=PART_ItemsTags, Path=Tag}" Value="True">
                                                                <Setter Property="MaxHeight" Value="{DynamicResource DE_FeaturesHeight}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ItemsControl.Style>

                                                <ItemsControl.Template>
                                                    <ControlTemplate TargetType="{x:Type ItemsControl}">
                                                        <Border x:Name="Border"
                                                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}">
                                                            <ScrollViewer Margin="0"
                                                                              Focusable="False"
                                                                              Padding="{TemplateBinding Padding}"
                                                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                              HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                                                              VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                                                              CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}">
                                                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                            </ScrollViewer>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter TargetName="Border" Property="Background" Value="Gray" />
                                                                <Setter TargetName="Border" Property="BorderBrush" Value="Silver" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </ItemsControl.Template>
                                            </ItemsControl>

                                            <Label Name="PART_ElemTags">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEC85;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCTagsLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsTags" Tag="{DynamicResource DE_LimitTagsHeight}"
                                                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                                                <ItemsControl.Style>
                                                    <Style TargetType="{x:Type ItemsControl}" BasedOn="{StaticResource {x:Type ItemsControl}}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ElementName=PART_ItemsTags, Path=Tag}" Value="True">
                                                                <Setter Property="MaxHeight" Value="{DynamicResource DE_TagsHeight}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ItemsControl.Style>

                                                <ItemsControl.Template>
                                                    <ControlTemplate TargetType="{x:Type ItemsControl}">
                                                        <Border x:Name="Border"
                                                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}">
                                                            <ScrollViewer Margin="0"
                                                                              Focusable="False"
                                                                              Padding="{TemplateBinding Padding}"
                                                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                              HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                                                              VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                                                              CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}">
                                                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                            </ScrollViewer>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter TargetName="Border" Property="Background" Value="Gray" />
                                                                <Setter TargetName="Border" Property="BorderBrush" Value="Silver" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </ItemsControl.Template>
                                            </ItemsControl>

                                            <Label Name="PART_ElemAgeRating">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCAgeRatingLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsAgeRatings" />

                                            <Label Name="PART_ElemSeries">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEEEF;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCSeriesLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsSeries" />

                                            <Label Name="PART_ElemSource">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEF65;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCSourceLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <Button Name="PART_ButtonSource" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemRegion">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xF02C;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCRegionLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsRegions" />

                                            <Label Name="PART_ElemVersion">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEFD1;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCVersionLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <Button Name="PART_ButtonVersion" VerticalAlignment="Center" />

                                            <Label Name="PART_ElemCommunityScore">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xF000;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCCommunityScore}" />
                                                </StackPanel>
                                            </Label>
                                            <TextBlock Name="PART_TextCommunityScore" VerticalAlignment="Center" Style="{DynamicResource TextBlockGameScore}" />

                                            <Label Name="PART_ElemCriticScore">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xF000;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCCriticScore}" />
                                                </StackPanel>
                                            </Label>
                                            <TextBlock Name="PART_TextCriticScore" VerticalAlignment="Center" Style="{DynamicResource TextBlockGameScore}" />

                                            <Label Name="PART_ElemUserScore">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xF000;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCUserScore}" />
                                                </StackPanel>
                                            </Label>
                                            <TextBlock Name="PART_TextUserScore" VerticalAlignment="Center" Style="{DynamicResource TextBlockGameScore}" />

                                            <Label Name="PART_ElemLinks">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEF71;" Style="{StaticResource Icon}" FontFamily="{StaticResource FontIcoFont}" Visibility="{DynamicResource DE_DetailsIconsVisibility}" />
                                                    <TextBlock Text="{DynamicResource LOCLinksLabel}" />
                                                </StackPanel>
                                            </Label>
                                            <ItemsControl Name="PART_ItemsLinks" />
                                        </GridEx>
                                    </StackPanel>

                                    <TabControl Margin="10,2,2,0">
                                        <TabItem Header="{DynamicResource LOCGameDescriptionTitle}" x:Name="DescriptionTab">
                                            <StackPanel Margin="10" x:Name="DescriptionContent">
                                                <!-- HowLongToBeat progress bar -->
                                                <StackPanel Visibility="{PluginSettings Plugin=HowLongToBeat, Path=EnableIntegrationProgressBar, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <StackPanel Visibility="{Binding ElementName=HowLongToBeat_PluginProgressBar, Path=Content.Visibility}" Margin="0,0,0,15">
                                                        <TextBlock Text="{DynamicResource LOCHowLongToBeat}" Style="{DynamicResource BaseTextBlockStyle}" />
                                                        <Separator Background="{DynamicResource PanelSeparatorBrush}" Margin="0,5,0,5" />
                                                        <ContentControl x:Name="HowLongToBeat_PluginProgressBar" Height="50" />
                                                    </StackPanel>
                                                </StackPanel>

                                                <!-- CheckLocalizations list languages -->
                                                <StackPanel Visibility="{PluginSettings Plugin=CheckLocalizations, Path=EnableIntegrationListLanguages, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <StackPanel Visibility="{Binding ElementName=CheckLocalizations_PluginListLanguages, Path=Content.Visibility}" Margin="0,0,0,15">
                                                        <TextBlock Text="{DynamicResource LOCCheckLocalizationsLocalizations}" Style="{DynamicResource BaseTextBlockStyle}" />
                                                        <Separator Background="{DynamicResource PanelSeparatorBrush}" Margin="0,5,0,5" />
                                                        <ContentControl x:Name="CheckLocalizations_PluginListLanguages" MinHeight="60" MaxHeight="300" />
                                                    </StackPanel>
                                                </StackPanel>

                                                <!-- ScreenshotsVisualizer single picture -->
                                                <StackPanel Visibility="{PluginSettings Plugin=ScreenshotsVisualizer, Path=EnableIntegrationShowSinglePicture, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <StackPanel Visibility="{Binding ElementName=ScreenshotsVisualizer_PluginSinglePicture, Path=Content.Visibility}" Margin="0,0,0,15">                                                
                                                        <TextBlock Text="{DynamicResource LOCSsvTitle}" Style="{DynamicResource BaseTextBlockStyle}" />
                                                        <Separator Background="{DynamicResource PanelSeparatorBrush}" Margin="0,5,0,5" />
                                                        <ContentControl x:Name="ScreenshotsVisualizer_PluginSinglePicture" MinHeight="100" MaxHeight="300" />
                                                    </StackPanel>
                                                </StackPanel>

                                                <!-- ScreenshotsVisualizer list pictures -->
                                                <StackPanel Visibility="{PluginSettings Plugin=ScreenshotsVisualizer, Path=EnableIntegrationShowPictures, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <StackPanel Visibility="{Binding ElementName=ScreenshotsVisualizer_PluginListScreenshots, Path=Content.Visibility}" Margin="0,0,0,15">
                                                        <TextBlock Text="{DynamicResource LOCSsvTitle}" Style="{DynamicResource BaseTextBlockStyle}" />
                                                        <Separator Background="{DynamicResource PanelSeparatorBrush}" Margin="0,5,0,5" />
                                                        <ContentControl x:Name="ScreenshotsVisualizer_PluginListScreenshots" MinHeight="50" MaxHeight="300" />
                                                    </StackPanel>
                                                </StackPanel>
                                                <StackPanel Visibility="{PluginSettings Plugin=ScreenshotsVisualizer, Path=EnableIntegrationShowPicturesVertical, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <StackPanel Visibility="{Binding ElementName=ScreenshotsVisualizer_PluginListScreenshotsVertical, Path=Content.Visibility}" Margin="0,0,0,15">
                                                        <TextBlock Text="{DynamicResource LOCSsvTitle}" Style="{DynamicResource BaseTextBlockStyle}" />
                                                        <Separator Background="{DynamicResource PanelSeparatorBrush}" Margin="0,5,0,5" />
                                                        <ContentControl x:Name="ScreenshotsVisualizer_PluginListScreenshotsVertical" MinHeight="50" Height="300" />
                                                    </StackPanel>
                                                </StackPanel>

                                                <StackPanel Name="PART_ElemDescription">
                                                    <HtmlTextView Name="PART_HtmlDescription"
                                                                  Margin="0,0,0,5"
                                                                  HtmlFontSize="{DynamicResource FontSize}"
                                                                  HtmlFontFamily="{DynamicResource FontFamily}"
                                                                  HtmlForeground="{DynamicResource TextColor}"
                                                                  LinkForeground="{DynamicResource GlyphColor}"                                      
                                                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                                  ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </TabItem>

                                        <TabItem Header="{DynamicResource LOCNotesLabel}">
                                            <TabItem.Style>
                                                <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=PART_TextNotes, Path=Text, Converter={StaticResource StringNullOrEmptyToBoolConverter}}" Value="False">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=PlayNotes, Path=IsControlVisible, FallbackValue=False}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TabItem.Style>

                                            <StackPanel Margin="10">
                                                <TextBox Name="PART_TextNotes" Margin="0,0,15,0"
                                                         IsReadOnly="True" BorderThickness="0" AcceptsReturn="True" TextWrapping="Wrap" />

                                                <ContentControl x:Name="PlayNotes_NotesViewerControl"
                                                                Visibility="{PluginSettings Plugin=PlayNotes, Path=IsControlVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" />
                                            </StackPanel>
                                        </TabItem>

                                        <TabItem Header="{DynamicResource LOCPaActivities}">
                                            <TabItem.Style>
                                                <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{PluginSettings Plugin=PlayerActivities, Path=EnableIntegrationActivities, FallbackValue=False}" Value="True" />
                                                                <Condition Binding="{PluginSettings Plugin=PlayerActivities, Path=HasData, FallbackValue=False}" Value="True" />
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TabItem.Style>

                                            <StackPanel Margin="10">
                                                <ContentControl x:Name="PlayerActivities_PluginActivities" MinHeight="500"
                                                                MaxHeight="{Binding ElementName=PART_GridEx, Path=ActualHeight}" />
                                            </StackPanel>
                                        </TabItem>

                                        <TabItem Header="{DynamicResource LOC_NewsViewer_NewsViewerControl_NewsLabel}"
                                                 Visibility="{PluginSettings Plugin=NewsViewer, Path=ReviewsAvailable, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <ContentControl x:Name="NewsViewer_NewsViewerControl" ClipToBounds="True" Margin="10" VerticalAlignment="Top" />
                                        </TabItem>

                                        <TabItem Header="{DynamicResource LOCReview_Viewer_ReviewsLabel}"
                                                 Visibility="{PluginSettings Plugin=ReviewViewer, Path=IsControlVisible, FallbackValue=Collapsed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <ContentControl x:Name="ReviewViewer_ReviewsControl" Margin="10" VerticalAlignment="Top" />
                                        </TabItem>

                                        <TabItem Header="{DynamicResource LOCExtra_Metadata_Loader_VideoLabel}">
                                            <TabItem.Style>
                                                <Style TargetType="{x:Type TabItem}" BasedOn="{StaticResource {x:Type TabItem}}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=IsAnyVideoAvailable, FallbackValue=False}" Value="True" />
                                                                <Condition Binding="{PluginSettings Plugin=ExtraMetadataLoader, Path=EnableVideoPlayer, FallbackValue=False}" Value="True" />
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TabItem.Style>

                                            <ContentControl x:Name="ExtraMetadataLoader_VideoLoaderControl" Margin="10" VerticalAlignment="Top">
                                                <ContentControl.Resources>
                                                    <Style x:Key="SliderThump" TargetType="{x:Type Thumb}">
                                                        <Setter Property="Background" Value="{DynamicResource NormalBrush}" />
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                                    <Grid>
                                                                        <Rectangle x:Name="HoverRectangle" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                                                                                               Width="Auto" Height="Auto" RadiusX="3" RadiusY="3"
                                                                                                               Stroke="{x:Null}" Margin="1,1,1,1" MinHeight="10"
                                                                                                               Fill="{TemplateBinding Background}"
                                                                                                               RenderTransformOrigin="0.5,0.5">
                                                                            <Rectangle.RenderTransform>
                                                                                <ScaleTransform ScaleX="1" ScaleY="1" x:Name="BorderScale"/>
                                                                            </Rectangle.RenderTransform>
                                                                        </Rectangle>
                                                                    </Grid>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsFocused" Value="True" />
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Fill" Value="{DynamicResource GlyphBrush}" TargetName="HoverRectangle" />
                                                                            <Trigger.EnterActions>
                                                                                <BeginStoryboard>
                                                                                    <Storyboard>
                                                                                        <DoubleAnimation Duration="0:0:0.1" To="1.01"
                                                                                                                                     Storyboard.TargetName="BorderScale"
                                                                                                                                     Storyboard.TargetProperty="ScaleX"
                                                                                                                                     />
                                                                                        <DoubleAnimation Duration="0:0:0.1" To="1.01"
                                                                                                                                     Storyboard.TargetName="BorderScale"
                                                                                                                                     Storyboard.TargetProperty="ScaleY"
                                                                                                                                     />
                                                                                    </Storyboard>
                                                                                </BeginStoryboard>
                                                                            </Trigger.EnterActions>
                                                                            <Trigger.ExitActions>
                                                                                <BeginStoryboard>
                                                                                    <Storyboard>
                                                                                        <DoubleAnimation Duration="0:0:0.1" To="1"
                                                                                                                                     Storyboard.TargetName="BorderScale"
                                                                                                                                     Storyboard.TargetProperty="ScaleX" 
                                                                                                                                     />
                                                                                        <DoubleAnimation Duration="0:0:0.1" To="1"
                                                                                                                                     Storyboard.TargetName="BorderScale"
                                                                                                                                     Storyboard.TargetProperty="ScaleY"
                                                                                                                                     />
                                                                                    </Storyboard>
                                                                                </BeginStoryboard>
                                                                            </Trigger.ExitActions>
                                                                        </Trigger>
                                                                        <DataTrigger Binding="{Binding IsFocused, RelativeSource={RelativeSource AncestorType=Track}}" Value="True">
                                                                            <Setter Property="Fill" Value="{DynamicResource GlyphBrush}" TargetName="HoverRectangle" />
                                                                        </DataTrigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                    <Style TargetType="{x:Type Slider}" BasedOn="{StaticResource BaseStyle}">
                                                        <Setter Property="SnapsToDevicePixels" Value="True" />
                                                        <Setter Property="Background" Value="{DynamicResource WindowBackgourndBrushBrightest}" />
                                                        <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}" />
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type Slider}">
                                                                    <Grid x:Name="GridRoot">
                                                                        <Grid.RowDefinitions>
                                                                            <RowDefinition Height="Auto" />
                                                                            <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                                                                            <RowDefinition Height="Auto" />
                                                                        </Grid.RowDefinitions>
                                                                        <TickBar x:Name="TopTick" Visibility="Collapsed" Height="4" Placement="Top"
                                                                                                 SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"  Fill="{DynamicResource GlyphBrush}" />
                                                                        <Border x:Name="Border" Grid.Row="1" Margin="0" Height="6" CornerRadius="{DynamicResource ControlCornerRadius}"
                                                                                                Background="{TemplateBinding Background}" BorderBrush="{DynamicResource NormalBorderBrush}"
                                                                                                BorderThickness="{DynamicResource ControlBorderThickness}" Opacity="0.5"/>
                                                                        <Track Grid.Row="1" x:Name="PART_Track" Height="{TemplateBinding Height}"
                                                                                               Effect="{DynamicResource TextDropShadow}"
                                                                                               UseLayoutRounding="True">
                                                                            <Track.Thumb>
                                                                                <Thumb Width="10" RenderTransformOrigin="0.5,0.5" x:Name="SliderThumb"
                                                                                                    Style="{DynamicResource SliderThump}">
                                                                                    <Thumb.RenderTransform>
                                                                                        <ScaleTransform ScaleX="1" ScaleY="1" x:Name="BorderScale"/>
                                                                                    </Thumb.RenderTransform>
                                                                                </Thumb>
                                                                            </Track.Thumb>
                                                                            <Track.DecreaseRepeatButton>
                                                                                <RepeatButton Style="{StaticResource SliderTrackButton}"
                                                                                                            Command="Slider.DecreaseLarge" />
                                                                            </Track.DecreaseRepeatButton>
                                                                            <Track.IncreaseRepeatButton>
                                                                                <RepeatButton Style="{StaticResource SliderTrackButton}"
                                                                                                            Command="Slider.IncreaseLarge" />
                                                                            </Track.IncreaseRepeatButton>
                                                                        </Track>
                                                                        <TickBar x:Name="BottomTick" Visibility="Collapsed" Grid.Row="2" Height="4" Placement="Bottom"
                                                                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Fill="{TemplateBinding Foreground}" />
                                                                    </Grid>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" SourceName="SliderThumb" Value="True">
                                                                            <Trigger.EnterActions>
                                                                                <BeginStoryboard>
                                                                                    <Storyboard>
                                                                                        <DoubleAnimation Duration="0:0:0.1" To="1.025"
                                                                                                                         Storyboard.TargetName="BorderScale"
                                                                                                                         Storyboard.TargetProperty="ScaleX"
                                                                                                                         />
                                                                                        <DoubleAnimation Duration="0:0:0.1" To="1.025"
                                                                                                                         Storyboard.TargetName="BorderScale"
                                                                                                                         Storyboard.TargetProperty="ScaleY"
                                                                                                                         />
                                                                                    </Storyboard>
                                                                                </BeginStoryboard>
                                                                            </Trigger.EnterActions>
                                                                            <Trigger.ExitActions>
                                                                                <BeginStoryboard>
                                                                                    <Storyboard>
                                                                                        <DoubleAnimation Duration="0:0:0.1" To="1"
                                                                                                                         Storyboard.TargetName="BorderScale"
                                                                                                                         Storyboard.TargetProperty="ScaleX" 
                                                                                                                         />
                                                                                        <DoubleAnimation Duration="0:0:0.1" To="1"
                                                                                                                         Storyboard.TargetName="BorderScale"
                                                                                                                         Storyboard.TargetProperty="ScaleY"
                                                                                                                         />
                                                                                    </Storyboard>
                                                                                </BeginStoryboard>
                                                                            </Trigger.ExitActions>
                                                                        </Trigger>
                                                                        <Trigger Property="IsFocused" Value="True">
                                                                            <Setter TargetName="PART_Track" Property="FocusManager.FocusedElement" Value="{Binding ElementName=PART_Track}" />
                                                                        </Trigger>
                                                                        <Trigger Property="TickPlacement" Value="TopLeft">
                                                                            <Setter Property="Visibility" Value="Visible" TargetName="TopTick" />
                                                                        </Trigger>
                                                                        <Trigger Property="TickPlacement" Value="BottomRight">
                                                                            <Setter Property="Visibility" Value="Visible" TargetName="BottomTick" />
                                                                        </Trigger>
                                                                        <Trigger Property="TickPlacement" Value="Both">
                                                                            <Setter Property="Visibility" Value="Visible" TargetName="TopTick" />
                                                                            <Setter Property="Visibility" Value="Visible" TargetName="BottomTick" />
                                                                        </Trigger>
                                                                        <Trigger Property="Orientation" Value="Vertical">
                                                                            <Setter Property="LayoutTransform" TargetName="GridRoot">
                                                                                <Setter.Value>
                                                                                    <RotateTransform Angle="-90" />
                                                                                </Setter.Value>
                                                                            </Setter>
                                                                            <Setter TargetName="PART_Track" Property="Orientation" Value="Horizontal" />
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ContentControl.Resources>
                                            </ContentControl>
                                        </TabItem>

                                        <!-- ScreenshotsVisualizer PluginScreenshots -->
                                        <TabItem>
                                            <TabItem.Style>
                                                <Style TargetType="{x:Type TabItem}" BasedOn="{StaticResource {x:Type TabItem}}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{PluginSettings Plugin=ScreenshotsVisualizer, Path=EnableIntegrationPicturesList, FallbackValue=False}" Value="True" />
                                                                <Condition Binding="{PluginSettings Plugin=ScreenshotsVisualizer, Path=HasData, FallbackValue=False}" Value="True" />
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TabItem.Style>

                                            <TabItem.Header>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <TextBlock Text="{DynamicResource LOCSsvTitle}" Tag="{StaticResource SSV_ShowCountImage}" Name="PART_SsvTab" />
                                                    <TextBlock Text="(" Margin="5,0,0,0" Visibility="{Binding ElementName=PART_SsvTab, Path=Tag, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                    <TextBlock Text="{PluginSettings Plugin=ScreenshotsVisualizer, Path=ListScreenshots.Count, Mode=OneWay, FallbackValue='0'}" Visibility="{Binding ElementName=PART_SsvTab, Path=Tag, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                    <TextBlock Text=")" Visibility="{Binding ElementName=PART_SsvTab, Path=Tag, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                </StackPanel>
                                            </TabItem.Header>

                                            <ContentControl x:Name="ScreenshotsVisualizer_PluginScreenshots" Margin="10" 
                                                            MinHeight="100" Height="500" VerticalAlignment="Top" />
                                        </TabItem>

                                        <TabItem Visibility="{PluginSettings Plugin=SuccessStory, Path=HasData, Mode=OneWay, FallbackValue={x:Static Visibility.Collapsed}}">
                                            <TabItem.Header>
                                                <StackPanel Orientation="Horizontal" Tag="{DynamicResource SS_ShowProgressBar}" Name="PART_SSTabHeader">
                                                    <TextBlock Text="{DynamicResource LOCSuccessStoryAchievements}" VerticalAlignment="Center" />

                                                    <ProgressBar Minimum="0" Maximum="100" Height="20" Width="80" Margin="5,0,0,0"
                                                                 VerticalAlignment="Center"
                                                                 Visibility="{Binding ElementName=PART_SSTabHeader, Path=Tag, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                 Value="{PluginSettings Plugin=SuccessStory, Path=Percent}" />

                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0,0,0"
                                                                Visibility="{Binding ElementName=PART_SSTabHeader, Path=Tag, Converter={StaticResource InvertedBooleanToVisibilityConverter}}">
                                                        <TextBlock Text="("  />
                                                        <TextBlock Text="{PluginSettings Plugin=SuccessStory, Path=Unlocked, Mode=OneWay, FallbackValue=''}" />
                                                        <TextBlock Text="/" />
                                                        <TextBlock Text="{PluginSettings Plugin=SuccessStory, Path=Total, Mode=OneWay, FallbackValue=''}" />
                                                        <TextBlock Text=")" />
                                                    </StackPanel>
                                                </StackPanel>
                                            </TabItem.Header>

                                            <StackPanel Margin="10">
                                                <ContentControl x:Name="SuccessStory_PluginProgressBar" />
                                                <ContentControl x:Name="SuccessStory_PluginChart" Margin="0,15,0,0" MinHeight="50" MaxHeight="200" />
                                                <ContentControl x:Name="SuccessStory_PluginCompactList" Margin="0,15,0,0" MaxHeight="150" />
                                                <ContentControl x:Name="SuccessStory_PluginCompactUnlocked" Margin="0,15,0,0" />
                                                <ContentControl x:Name="SuccessStory_PluginCompactLocked" Margin="0,15,0,0" MaxHeight="150" />
                                                <ContentControl x:Name="SuccessStory_PluginList" Margin="0,15,0,0" MinHeight="50" MaxHeight="500" />
                                                <ContentControl x:Name="SuccessStory_PluginUserStats" Margin="0,15,0,0" MinHeight="50" MaxHeight="200" />
                                            </StackPanel>
                                        </TabItem>

                                        <TabItem Header="{DynamicResource LOCGameActivityTitle}"
                                                 Visibility="{PluginSettings Plugin=GameActivity, Path=HasData, Mode=OneWay, FallbackValue={x:Static Visibility.Collapsed}}">
                                            <StackPanel Margin="10">
                                                <!-- GameActivity time chart -->
                                                <ContentControl x:Name="GameActivity_PluginChartTime" MinHeight="50" MaxHeight="300" />

                                                <!-- GameActivity log chart -->
                                                <ContentControl x:Name="GameActivity_PluginChartLog" MinHeight="50" MaxHeight="300" Margin="0,15,0,0" />
                                            </StackPanel>
                                        </TabItem>

                                        <TabItem Header="{DynamicResource LOCGameRelations_RelatedGamesLabel}" Visibility="{Binding ElementName=GameRelationsVisibilityProxy, Path=Tag}">
                                            <StackPanel Orientation="Vertical" Margin="10">
                                                <Expander x:Name="Panel_GameRelations_SimilarGamesControl"
                                                          Header="{DynamicResource LOCGameRelations_SimilarGamesLabel}"
                                                          IsExpanded="True"
                                                          Visibility="{PluginSettings Plugin=GameRelations, Path=SimilarGamesControlSettings.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                                                    <ContentControl x:Name="GameRelations_SimilarGamesControl" Margin="0,10,0,0" />
                                                </Expander>

                                                <Expander x:Name="Panel_GameRelations_SameSeriesControl"
                                                          Header="{DynamicResource LOCGameRelations_SameSeriesLabel}"
                                                          IsExpanded="True"
                                                          Margin="0,15,0,0"
                                                          Visibility="{PluginSettings Plugin=GameRelations, Path=SameSeriesControlSettings.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                                                    <ContentControl x:Name="GameRelations_SameSeriesControl" Margin="0,10,0,0" />
                                                </Expander>

                                                <Expander x:Name="Panel_GameRelations_SameDeveloperControl"
                                                          Header="{DynamicResource LOCGameRelations_SameDeveloperLabel}"
                                                          IsExpanded="True"
                                                          Margin="0,15,0,0"
                                                          Visibility="{PluginSettings Plugin=GameRelations, Path=SameDeveloperControlSettings.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                                                    <ContentControl x:Name="GameRelations_SameDeveloperControl" Margin="0,10,0,0" />
                                                </Expander>

                                                <Expander x:Name="Panel_GameRelations_SamePublisherControl"
                                                          Header="{DynamicResource LOCGameRelations_SamePublisherLabel}"
                                                          IsExpanded="True"
                                                          Margin="0,15,0,0"
                                                          Visibility="{PluginSettings Plugin=GameRelations, Path=SamePublisherControlSettings.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                                                    <ContentControl x:Name="GameRelations_SamePublisherControl" Margin="0,10,0,0" />
                                                </Expander>
                                            </StackPanel>
                                        </TabItem>
                                    </TabControl>
                                </DockPanel>
                            </Grid>
                        </Grid>
                    </ScrollViewer>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DuplicateHider_ContentControlHeader_Style" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Grid>
                        <!--
                            DataContext has an ObservableCollection called Games
                            containing all copys of the Game in the current context (inlcuding itself).
                            Each entry is of Type
                            ListData {
                                Playnite.SDK.Models.Game Game;
                                Boolean IsCurrent;  // True if this copy eqal to the current GameContext property.
                                BitmapImage Icon;   // Source Icon
                                String SourceName;  // Source name. Use this rather than Game.Source.Name, 
                                                    // because Source might be null.
                                ICommand LaunchCommand;
                                ICommand SelectCommand;
                                ICommand InstallCommand;
                                ICommand UninstallCommand;
                            }
                        -->
                        <ItemsControl x:Name="DH_ListBox" Focusable="False" IsTextSearchEnabled="False" 
                                      ItemsSource="{Binding Games}" 
                                      VirtualizingPanel.VirtualizationMode="Recycling">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,3,0,0">
                                        <!--  StackPanel containing icon and source name of a game.  -->
                                        <StackPanel x:Name="_Item" Background="Transparent" Orientation="Horizontal">
                                            <Image MaxHeight="20" Margin="5,0,5,0"
                                                   VerticalAlignment="Center" DockPanel.Dock="Left"
                                                   RenderOptions.BitmapScalingMode="Fant">
                                                <Image.Style>
                                                    <Style TargetType="{x:Type Image}">
                                                        <Setter Property="Source" Value="{Binding Icon}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Value="Indiegala" Binding="{Binding SourceName}">
                                                                <Setter Property="Source" Value="{StaticResource DuplicateHider_Indiegala_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="Legacy Games" Binding="{Binding SourceName}">
                                                                <Setter Property="Source" Value="{StaticResource DuplicateHider_LegacyGames_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="Riot" Binding="{Binding SourceName}">
                                                                <Setter Property="Source" Value="{StaticResource DuplicateHider_Riot_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="Riot Games" Binding="{Binding SourceName}">
                                                                <Setter Property="Source" Value="{StaticResource DuplicateHider_Riot_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="Switch" Binding="{Binding SourceName}">
                                                                <Setter Property="Source" Value="{StaticResource DuplicateHider_Switch_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="EA app" Binding="{Binding SourceName}">
                                                              <Setter Property="Source" Value="{StaticResource DuplicateHider_EAapp_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="Hacked" Binding="{Binding SourceName}">
                                                              <Setter Property="Source" Value="{StaticResource DuplicateHider_Devil_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="Android" Binding="{Binding SourceName}">
                                                              <Setter Property="Source" Value="{StaticResource DuplicateHider_Android_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="Oculus" Binding="{Binding SourceName}">
                                                                <Setter Property="Source" Value="{StaticResource DuplicateHider_Oculus_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="Microsoft Store" Binding="{Binding SourceName}">
                                                                <Setter Property="Source" Value="{StaticResource DuplicateHider_MicrosoftStore_Icon}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Value="JAST USA" Binding="{Binding SourceName}">
                                                                <Setter Property="Source" Value="{StaticResource DuplicateHider_JastUSA_Icon}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                                <Image.Effect>
                                                    <DropShadowEffect Color="Black" Opacity="0.75" BlurRadius="5" ShadowDepth="0" />
                                                </Image.Effect>
                                            </Image>
                                            <TextBlock x:Name="_SourceText" Margin="5,0,5,0" VerticalAlignment="Center" 
                                                       Foreground="White" Text="{Binding SourceName}" Style="{DynamicResource BaseTextBlockStyle}">
                                                <TextBlock.Effect>
                                                    <DropShadowEffect Color="Black" Opacity="0.75" BlurRadius="5" ShadowDepth="0" />
                                                </TextBlock.Effect>
                                            </TextBlock>
                                            <!--  Binding gestures to commands.  -->
                                            <StackPanel.InputBindings>
                                                <MouseBinding Command="{Binding LaunchCommand}" Gesture="LeftDoubleClick" />
                                                <MouseBinding Command="{Binding SelectCommand}" Gesture="LeftClick" />
                                            </StackPanel.InputBindings>
                                        </StackPanel>
                                        <!--  Rectangle used to indicate the currently selected copy.  -->
                                        <Rectangle x:Name="_Rect" Height="2" Margin="0,3,0,0" 
                                                   VerticalAlignment="Bottom" RenderTransformOrigin="0.5,0" 
                                                   Visibility="Visible">
                                            <Rectangle.Fill>
                                                <SolidColorBrush Color="{StaticResource GlyphColor}" />
                                            </Rectangle.Fill>
                                            <Rectangle.RenderTransform>
                                                <ScaleTransform x:Name="_RectScale" ScaleX="0" />
                                            </Rectangle.RenderTransform>
                                        </Rectangle>
                                    </StackPanel>
                                    <DataTemplate.Triggers>
                                        <!--  Workaround to prevent the animation when switching to a copy of a game for the first time  -->
                                        <!--#region Workaround-->
                                        <DataTrigger Binding="{Binding DataContext.SwitchedGroup, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}" Value="True">
                                            <Setter Property="Rectangle.Visibility" Value="{Binding IsCurrent, Converter={StaticResource BooleanToVisibilityConverter}}" TargetName="_Rect"/>
                                            <Setter Property="Rectangle.RenderTransform" TargetName="_Rect">
                                                <Setter.Value>
                                                    <ScaleTransform x:Name="_RectScale1" ScaleX="1" />
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <!--#endregion-->
                                        <!--  Animation for the selection bar below the selected item.  -->
                                        <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="_RectScale"
                                                                         Storyboard.TargetProperty="(ScaleTransform.ScaleX)"
                                                                         To="1" Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CubicEase />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="_RectScale"
                                                                         Storyboard.TargetProperty="(ScaleTransform.ScaleX)"
                                                                         To="0" Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CubicEase />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <!--  MouseOver glow effect.  -->
                                            <Setter TargetName="_Item" Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect BlurRadius="10" Opacity="1"
                                                                      ShadowDepth="0" Color="White" />
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                        <!--  Make items for unisntalled games slightly transparent.  -->
                                        <DataTrigger Binding="{Binding Game.IsInstalled}" Value="False">
                                            <Setter TargetName="_Item" Property="Opacity" Value="0.5" />
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
    